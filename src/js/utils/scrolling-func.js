export const scrollToTop = function () {
  window.scrollTo({
    top: 700,
    behavior: 'smooth',
  });
};

export const scrollToBottom = function () {
  window.scrollTo({
    top: 700,
    behavior: 'smooth',
  });
};
